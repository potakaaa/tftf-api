{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/run-python.mjs -m uvicorn app.main:app --reload --no-access-log --port 8000",
    "native:build": "node scripts/build-native.mjs",
    "test": "node scripts/run-python.mjs -m pytest"
  }
}
